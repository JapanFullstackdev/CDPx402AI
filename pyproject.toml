[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-defi-agent"
version = "0.2.0"
description = "AI DeFi research agent using Strands, Coinbase AgentKit, CDP wallets, and x402."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
